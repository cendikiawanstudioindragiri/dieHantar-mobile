
import 'package:flutter/material.dart';

class SignupEmptyScreen extends StatelessWidget {
  const SignupEmptyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Signup Empty Screen'),
      ),
    );
  }
}
