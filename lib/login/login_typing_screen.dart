import 'package:flutter/material.dart';

class LoginTypingScreen extends StatelessWidget {
  const LoginTypingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Login Typing Screen')));
  }
}
