
import 'package:flutter/material.dart';

class HomeFilledScreen extends StatelessWidget {
  const HomeFilledScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Filled Screen'),
      ),
    );
  }
}
