import 'package:flutter/material.dart';

class ServicesAddBasketScreen extends StatelessWidget {
  const ServicesAddBasketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Services Add Basket'),
      ),
      body: const Center(
        child: Text('Services Add Basket Screen'),
      ),
    );
  }
}
