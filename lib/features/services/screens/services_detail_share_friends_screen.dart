import 'package:flutter/material.dart';

class ServicesDetailShareFriendsScreen extends StatelessWidget {
  const ServicesDetailShareFriendsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Services Detail Share Friends'),
      ),
      body: const Center(
        child: Text('Services Detail Share Friends Screen'),
      ),
    );
  }
}
