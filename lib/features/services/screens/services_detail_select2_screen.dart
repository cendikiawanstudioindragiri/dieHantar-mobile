import 'package:flutter/material.dart';

class ServicesDetailSelect2Screen extends StatelessWidget {
  const ServicesDetailSelect2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Services Detail Select 2'),
      ),
      body: const Center(
        child: Text('Services Detail Select 2 Screen'),
      ),
    );
  }
}
