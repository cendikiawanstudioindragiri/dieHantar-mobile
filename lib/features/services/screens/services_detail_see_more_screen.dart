import 'package:flutter/material.dart';

class ServicesDetailSeeMoreScreen extends StatelessWidget {
  const ServicesDetailSeeMoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Services Detail See More'),
      ),
      body: const Center(
        child: Text('Services Detail See More Screen'),
      ),
    );
  }
}
