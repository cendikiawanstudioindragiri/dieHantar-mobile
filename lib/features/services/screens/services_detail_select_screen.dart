import 'package:flutter/material.dart';

class ServicesDetailSelectScreen extends StatelessWidget {
  const ServicesDetailSelectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Services Detail Select'),
      ),
      body: const Center(
        child: Text('Services Detail Select Screen'),
      ),
    );
  }
}
