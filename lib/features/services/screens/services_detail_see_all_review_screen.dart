import 'package:flutter/material.dart';

class ServicesDetailSeeAllReviewScreen extends StatelessWidget {
  const ServicesDetailSeeAllReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Services Detail See All Review'),
      ),
      body: const Center(
        child: Text('Services Detail See All Review Screen'),
      ),
    );
  }
}
