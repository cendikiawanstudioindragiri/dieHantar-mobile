import 'package:flutter/material.dart';

class PaymentAddNewCardTypingCvv2Screen extends StatelessWidget {
  const PaymentAddNewCardTypingCvv2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Typing CVV2'),
      ),
      body: const Center(
        child: Text('Typing CVV2 Screen'),
      ),
    );
  }
}
