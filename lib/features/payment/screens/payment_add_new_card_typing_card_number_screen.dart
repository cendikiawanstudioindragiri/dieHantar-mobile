import 'package:flutter/material.dart';

class PaymentAddNewCardTypingCardNumberScreen extends StatelessWidget {
  const PaymentAddNewCardTypingCardNumberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Typing Card Number'),
      ),
      body: const Center(
        child: Text('Typing Card Number Screen'),
      ),
    );
  }
}
