import 'package:flutter/material.dart';

class PaymentAddNewCardTypingCardholderNameScreen extends StatelessWidget {
  const PaymentAddNewCardTypingCardholderNameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Typing Cardholder Name'),
      ),
      body: const Center(
        child: Text('Typing Cardholder Name Screen'),
      ),
    );
  }
}
