import 'package:flutter/material.dart';

class PaymentAddNewCardTypingCvvScreen extends StatelessWidget {
  const PaymentAddNewCardTypingCvvScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Typing CVV'),
      ),
      body: const Center(
        child: Text('Typing CVV Screen'),
      ),
    );
  }
}
