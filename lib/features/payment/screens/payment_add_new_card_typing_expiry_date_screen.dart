import 'package:flutter/material.dart';

class PaymentAddNewCardTypingExpiryDateScreen extends StatelessWidget {
  const PaymentAddNewCardTypingExpiryDateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Typing Expiry Date'),
      ),
      body: const Center(
        child: Text('Typing Expiry Date Screen'),
      ),
    );
  }
}
