import 'package:flutter/material.dart';

class LikedSearch2Screen extends StatelessWidget {
  const LikedSearch2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Liked Search 2'),
      ),
      body: const Center(
        child: Text('Liked Search 2 Screen'),
      ),
    );
  }
}
