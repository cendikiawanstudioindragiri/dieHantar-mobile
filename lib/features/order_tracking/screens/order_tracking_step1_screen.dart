import 'package:flutter/material.dart';

class OrderTrackingStep1Screen extends StatelessWidget {
  const OrderTrackingStep1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Order Tracking Step 1'),
      ),
      body: const Center(
        child: Text('Order Tracking Step 1 Screen'),
      ),
    );
  }
}
