import 'package:flutter/material.dart';

class OrderTrackingStep4Screen extends StatelessWidget {
  const OrderTrackingStep4Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Order Tracking Step 4'),
      ),
      body: const Center(
        child: Text('Order Tracking Step 4 Screen'),
      ),
    );
  }
}
