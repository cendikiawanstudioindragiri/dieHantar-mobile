import 'package:flutter/material.dart';

class OrderTrackingStep3Screen extends StatelessWidget {
  const OrderTrackingStep3Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Order Tracking Step 3'),
      ),
      body: const Center(
        child: Text('Order Tracking Step 3 Screen'),
      ),
    );
  }
}
