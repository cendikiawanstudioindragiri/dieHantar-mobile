import 'package:flutter/material.dart';

class OrderTrackingStep2Screen extends StatelessWidget {
  const OrderTrackingStep2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Order Tracking Step 2'),
      ),
      body: const Center(
        child: Text('Order Tracking Step 2 Screen'),
      ),
    );
  }
}
