import 'package:flutter/material.dart';

class MyBasket2Screen extends StatelessWidget {
  const MyBasket2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('My Basket 2'),
      ),
      body: const Center(
        child: Text('My Basket 2 Screen'),
      ),
    );
  }
}
