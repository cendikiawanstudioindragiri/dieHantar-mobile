import 'package:flutter/material.dart';

class DrinksDetailSelectScreen extends StatelessWidget {
  const DrinksDetailSelectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Drinks Detail Select'),
      ),
      body: const Center(
        child: Text('Drinks Detail Select Screen'),
      ),
    );
  }
}
