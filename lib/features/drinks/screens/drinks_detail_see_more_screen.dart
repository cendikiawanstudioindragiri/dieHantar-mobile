import 'package:flutter/material.dart';

class DrinksDetailSeeMoreScreen extends StatelessWidget {
  const DrinksDetailSeeMoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Drinks Detail See More'),
      ),
      body: const Center(
        child: Text('Drinks Detail See More Screen'),
      ),
    );
  }
}
