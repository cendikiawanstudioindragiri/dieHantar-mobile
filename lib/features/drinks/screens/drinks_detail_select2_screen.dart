import 'package:flutter/material.dart';

class DrinksDetailSelect2Screen extends StatelessWidget {
  const DrinksDetailSelect2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Drinks Detail Select 2'),
      ),
      body: const Center(
        child: Text('Drinks Detail Select 2 Screen'),
      ),
    );
  }
}
