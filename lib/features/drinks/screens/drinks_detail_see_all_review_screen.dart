import 'package:flutter/material.dart';

class DrinksDetailSeeAllReviewScreen extends StatelessWidget {
  const DrinksDetailSeeAllReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Drinks Detail See All Review'),
      ),
      body: const Center(
        child: Text('Drinks Detail See All Review Screen'),
      ),
    );
  }
}
