import 'package:flutter/material.dart';

class DrinksDetailShareFriendsScreen extends StatelessWidget {
  const DrinksDetailShareFriendsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Drinks Detail Share Friends'),
      ),
      body: const Center(
        child: Text('Drinks Detail Share Friends Screen'),
      ),
    );
  }
}
