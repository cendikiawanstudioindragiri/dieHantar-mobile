import 'package:flutter/material.dart';

class CancelOrderOtherReasonsScreen extends StatelessWidget {
  const CancelOrderOtherReasonsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Other Reasons'),
      ),
      body: const Center(
        child: Text('Other Reasons Screen'),
      ),
    );
  }
}
