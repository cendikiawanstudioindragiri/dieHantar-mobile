import 'package:flutter/material.dart';

class ProfileChangeUserInformationScreen extends StatelessWidget {
  const ProfileChangeUserInformationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Change User Information'),
      ),
      body: const Center(
        child: Text('Change User Information Screen'),
      ),
    );
  }
}
