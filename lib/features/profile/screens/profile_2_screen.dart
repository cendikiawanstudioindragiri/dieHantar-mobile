import 'package:flutter/material.dart';

class Profile2Screen extends StatelessWidget {
  const Profile2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Profile 2'),
      ),
      body: const Center(
        child: Text('Profile 2 Screen'),
      ),
    );
  }
}
