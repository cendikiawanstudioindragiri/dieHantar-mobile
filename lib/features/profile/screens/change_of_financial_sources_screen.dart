import 'package:flutter/material.dart';

class ChangeOfFinancialSourcesScreen extends StatelessWidget {
  const ChangeOfFinancialSourcesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Change of Financial Sources'),
      ),
      body: const Center(
        child: Text('Change of Financial Sources Screen'),
      ),
    );
  }
}
