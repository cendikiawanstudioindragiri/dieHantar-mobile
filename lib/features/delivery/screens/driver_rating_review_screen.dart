import 'package:flutter/material.dart';

class DriverRatingReviewScreen extends StatelessWidget {
  const DriverRatingReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Driver Rating Review'),
      ),
      body: const Center(
        child: Text('Driver Rating Review Screen'),
      ),
    );
  }
}
