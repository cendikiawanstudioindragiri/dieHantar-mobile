import 'package:flutter/material.dart';

class RateYourMealReviewScreen extends StatelessWidget {
  const RateYourMealReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rate Your Meal Review'),
      ),
      body: const Center(
        child: Text('Rate Your Meal Review Screen'),
      ),
    );
  }
}
