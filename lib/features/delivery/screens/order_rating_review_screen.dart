import 'package:flutter/material.dart';

class OrderRatingReviewScreen extends StatelessWidget {
  const OrderRatingReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Order Rating Review'),
      ),
      body: const Center(
        child: Text('Order Rating Review Screen'),
      ),
    );
  }
}
