import 'package:flutter/material.dart';

class RateYourMealNotificationScreen extends StatelessWidget {
  const RateYourMealNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rate Your Meal Notification'),
      ),
      body: const Center(
        child: Text('Rate Your Meal Notification Screen'),
      ),
    );
  }
}
