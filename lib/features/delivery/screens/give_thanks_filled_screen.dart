import 'package:flutter/material.dart';

class GiveThanksFilledScreen extends StatelessWidget {
  const GiveThanksFilledScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Give Thanks Filled'),
      ),
      body: const Center(
        child: Text('Give Thanks Filled Screen'),
      ),
    );
  }
}
