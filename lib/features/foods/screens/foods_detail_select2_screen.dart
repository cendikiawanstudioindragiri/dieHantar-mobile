import 'package:flutter/material.dart';

class FoodsDetailSelect2Screen extends StatelessWidget {
  const FoodsDetailSelect2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Foods Detail Select 2'),
      ),
      body: const Center(
        child: Text('Foods Detail Select 2 Screen'),
      ),
    );
  }
}
