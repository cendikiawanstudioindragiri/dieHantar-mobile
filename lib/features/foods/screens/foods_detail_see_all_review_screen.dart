import 'package:flutter/material.dart';

class FoodsDetailSeeAllReviewScreen extends StatelessWidget {
  const FoodsDetailSeeAllReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Foods Detail See All Review'),
      ),
      body: const Center(
        child: Text('Foods Detail See All Review Screen'),
      ),
    );
  }
}
