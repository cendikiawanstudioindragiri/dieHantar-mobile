import 'package:flutter/material.dart';

class FoodsDetailShareFriendsScreen extends StatelessWidget {
  const FoodsDetailShareFriendsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Foods Detail Share Friends'),
      ),
      body: const Center(
        child: Text('Foods Detail Share Friends Screen'),
      ),
    );
  }
}
