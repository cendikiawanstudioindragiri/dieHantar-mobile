import 'package:flutter/material.dart';

class ReportAccountAndBugScreen extends StatelessWidget {
  const ReportAccountAndBugScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Report Account & Bug'),
      ),
      body: const Center(
        child: Text('Report Account & Bug Screen'),
      ),
    );
  }
}
