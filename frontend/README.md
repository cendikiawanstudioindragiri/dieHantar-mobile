Mobile app (placeholder)

This folder is a placeholder for the mobile client (React Native / Expo by default).

Quick start (Expo):

- Install:

```bash
cd mobile
npm install
```

- Start:

```bash
npx expo start
```

Replace this placeholder with your real mobile app when ready.
